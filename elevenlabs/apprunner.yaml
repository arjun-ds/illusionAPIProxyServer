version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python3 -m uvicorn elevenlabs_app:app --host 0.0.0.0 --port 8001
  network:
    port: 8001
    env: PORT
  env:
    - name: ELEVENLABS_API_KEY
      value: ${ELEVENLABS_API_KEY}
